import Navigation from './Navigation'

/**
 * Header
 * @component
 * @category Common
 */
const Header = () => {
    return (
        <nav>
            <Navigation />
        </nav>
    )
}

export default Header
