import Logo from './Logo'
import Toolbar from './Toolbar'

/**
 * Header ot the application
 * @component
 * @category Common
 */
const Header = () => {
    return (
        <header>
            <Logo />
            <Toolbar />
        </header>
    )
}

export default Header
